#!/usr/bin/env sh

printf 'Open images recursively? (y/N): '
read -r resp

if [ "${resp}" = "y" ]; then
  vimiv --shuffle --recursive > /dev/null 2>&1 &
else
  vimiv --shuffle ./* > /dev/null 2>&1 &
fi
