#!/usr/bin/sh

if [ -d "${nnn}" ]; then
    find "${nnn}" -type f \
      -not -path '*/.*' \
      -not '(' -iname '*.lrc' -or -iname '*.jpg' -or -iname '*.png' ')' \
      -exec mpv --profile=music '{}' '+' &
else
    mpv --profile=music "${nnn}" &
fi
